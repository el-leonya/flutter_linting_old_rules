name: flutter_linting_rules
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.1.0

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter_lints: ^2.0.0
dev_dependencies:
  dart_code_metrics: ^4.19.1
